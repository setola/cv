version: "3"

networks:
  cv:
    external: false

services:
  node:
    networks:
      - cv
    build:
      context: .
      dockerfile: ./docker/node/Dockerfile
    env_file:
      - .env
    volumes:
      - ./src:$APP_ROOT
